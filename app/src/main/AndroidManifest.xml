<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.IntervalTimer">

        <!--
            Handle fold/unfold and rotation in-process. Without this the Flip's fold destroys the
            Activity, and the editor's in-progress sequence (plain `remember` state) is gone. Compose
            re-lays out and refreshes LocalDensity/LocalConfiguration from onConfigurationChanged,
            and hideSystemBars is re-applied via onWindowFocusChanged. This is a mitigation, not
            persistence: it does not cover process death or a font-scale change.
        -->
        <!--
            Portrait only. The whole layout — perimeter progress arms, the auto-fitted clock, the
            cover-screen budget — is built for a tall window; landscape has nothing to gain and a
            timer that flips mid-set while you're moving is worse than useless. configChanges stays:
            it still catches the fold, which is a size change, not a rotation.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|uiMode|keyboardHidden"
            android:theme="@style/Theme.IntervalTimer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.TimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="interval_timer" />
        </service>

    </application>
</manifest>
